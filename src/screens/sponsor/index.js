import React from "react";
import { Text, View } from "react-native";
import { Header } from "../../components";

const SponsorScreen = () => {
  return (
    <View>
      <Header title={"પ્રયોજક"} isBack={true} isRight={true} isFiler={true} />
    </View>
  );
};

export default SponsorScreen;
